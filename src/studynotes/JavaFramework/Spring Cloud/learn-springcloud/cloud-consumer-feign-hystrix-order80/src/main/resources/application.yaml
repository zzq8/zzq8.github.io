server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

#开启   坑你因为这个消费方用的是 feign 调用
feign:
  hystrix:
    enabled: true
