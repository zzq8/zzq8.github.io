server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址

#把监控的东西暴露出来
management:
  endpoints:
    web:
      exposure:
        include: '*'